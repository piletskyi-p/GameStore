namespace GameStore.Bll.Observer
{
    public class SmsSender : BaseSender
    {
        public override void Send(Manager manager, UserInfo userInfo, OrderModel order)
        {
            throw new System.NotImplementedException();
        }
    }
}